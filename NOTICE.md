# Hinweise

Soriono Prelude liefert reproduzierbare Suche, Datenzugriff und lokale
SQL-Berechnung. Ergebnisse müssen fachlich geprüft werden. Öffentliche
Datenanbieter erhalten beim Abruf normale HTTP-Anfragen; lokale Pfade und
gespeicherte Resultate werden nicht an Soriono übertragen.
